# Advent of Code Server Configuration
# Copy this file to .env and update with your settings

# Node.js binary path (find with: which node)
# Common locations:
#   - /usr/local/bin/node (Homebrew)
#   - /opt/homebrew/bin/node (Homebrew on Apple Silicon)
#   - ~/.nvm/versions/node/vX.X.X/bin/node (nvm)
NODE_PATH=/usr/local/bin/node

# Server port
SERVER_PORT=23030

# Session cookie for Advent of Code
# Get from: https://adventofcode.com (login, then check cookies)
# This should be stored in .session file (one line, just the cookie value)
# Leave this line commented - we use .session file instead
# SESSION_COOKIE=your_session_cookie_here
